# ATP-ServeBot ML tier — SAM 3D Body on Apple Silicon (SERVEBOT_PIPELINE=sam3d).
#
# Install ON TOP of requirements.txt, in a Python 3.12 venv:
#     pip install -r requirements.txt -r requirements-ml.txt
#
# torch is pinned to the version proven by the MPS spike (fp32 on M1 Pro).
# The base requirements.txt stays torch-free — the stub pipeline (default)
# must import and run without any of this.
torch==2.12.1
torchvision==0.27.1
opencv-python>=4.10
pytorch-lightning>=2.6
timm>=1.0
trimesh>=4.6
roma>=1.5
hydra-core>=1.3
hydra-colorlog>=1.2
einops>=0.8
yacs>=0.1.8
scikit-image>=0.25
dill>=0.4
loguru>=0.7
optree>=0.13
fvcore>=0.1.5
omegaconf>=2.3
braceexpand>=0.1.7
webdataset>=0.2
huggingface_hub>=0.30
pyrootutils>=1.0
ultralytics>=8.4      # YOLO ball/racket/person detection + dense 2D pose (phase-2 tracking)
